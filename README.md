# masterrepo
#include<stdio.h>
int main()
{
    int a= 5;
    int b =6;
    printf("the total summmm is= %d", b+a);
    printf("the diff is= %d", b-a);
    printf("the multiplication value is %d", a*b);
}
